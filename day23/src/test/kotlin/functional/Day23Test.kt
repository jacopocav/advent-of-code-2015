package functional

import ceejay.advent.Day23
import ceejay.advent.util.Input
import org.junit.jupiter.api.Disabled
import kotlin.test.Test
import kotlin.test.assertEquals

@Disabled
class Day23Test {

    @Test
    fun `Part 1 returns correct result on example input`() {
        // when
        val (actual, _) = Day23.part1(Input.Example)

        // then
        assertEquals(TODO(), actual)
    }

    @Test
    fun `Part 1 returns correct result on real input`() {
        // when
        val (actual, _) = Day23.part1(Input.Real)

        // then
        assertEquals(TODO(), actual)
    }

    @Test
    fun `Part 2 returns correct result on example input`() {
        // when
        val (actual, _) = Day23.part2(Input.Example)

        // then
        assertEquals(TODO(), actual)
    }

    @Test
    fun `Part 2 returns correct result on real input`() {
        // when
        val (actual, _) = Day23.part2(Input.Real)

        // then
        assertEquals(TODO(), actual)
    }
}